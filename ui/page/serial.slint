export component Serial inherits Rectangle {
    background: #3c16e6;
    animate background {
        duration: 500ms;
    }
    Text { 
        color: #0a0a0a;
        text: "Serial 페이지!"; 
        animate color {
            duration: 500ms;
        }
    }
}